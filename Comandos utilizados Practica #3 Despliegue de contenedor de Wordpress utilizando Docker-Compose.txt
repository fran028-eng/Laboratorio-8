Comandos utilizados Practica #3: Despliegue de contenedor de Wordpress utilizando Docker-Compose 

---Instalar Docker Compose---

# 1. Descargar Docker Compose binario (versión específica estable)
sudo curl -L "https://github.com/docker/compose/releases/download/v2.27.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# 2. Dar permisos
sudo chmod +x /usr/local/bin/docker-compose

# 3. Verificar instalación
docker-compose --version


---Configurar y desplegar WordPress---

# 1. Crear carpeta de proyecto
mkdir ~/wordpress-docker
cd ~/wordpress-docker

# 2. Crear archivo docker-compose.yml
nano docker-compose.yml

---Contenido---


version: '3.8'

services:
  wordpress:
    image: wordpress:6.5
    ports:
      - "8080:80"
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME}
    volumes:
      - wordpress_data:/var/www/html
    depends_on:
      - db

  db:
    image: mysql:5.7.44
    environment:
      MYSQL_DATABASE: ${WORDPRESS_DB_NAME}
      MYSQL_USER: ${WORDPRESS_DB_USER}
      MYSQL_PASSWORD: ${WORDPRESS_DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql

volumes:
  wordpress_data:
  db_data:


-----------------------


---#Archivo .env (en la misma carpeta)
WORDPRESS_DB_USER=francisco
WORDPRESS_DB_PASSWORD=12345
WORDPRESS_DB_NAME=wordpressdb
MYSQL_ROOT_PASSWORD=rootpass



----Despliegue

# 3. Levantar contenedores
docker-compose up -d

# 4. Verificar que corren
docker ps

# 5. Revisar logs si algo falla
docker-compose logs -f


----Acceder a WordPress----

Navegador → http://159.223.169.229:8080

Configurar idioma, usuario admin, contraseña, email.

Guardar y acceder al panel de administración.


